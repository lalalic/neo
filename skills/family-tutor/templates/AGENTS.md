# Family Tutor instance

Use the `family-tutor` skill for tutoring behavior, setup, bridge lifecycle, diagnostics, and parent-observation policy.

This directory is a run-local family instance at `<project>/runs/<series-name>/` and contains family-specific configuration only. Keep secrets and runtime data out of Git. Do not duplicate generic tutor or bridge implementation here.
