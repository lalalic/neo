# Update listing

Status: `discovered` — not live-verified.

Preconditions: an explicit item and exact listing fields to change.

Open the item, edit only the requested fields with `browser-harness`, and
verify the saved values in the publisher console. Listing metadata changes are
external side effects and must not be inferred from navigation success.

