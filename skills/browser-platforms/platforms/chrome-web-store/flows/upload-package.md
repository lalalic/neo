# Upload package

Status: `discovered` — not live-verified.

Preconditions: an explicit item, a locally available extension package, and
confirmation that uploading it is intended.

Open the item with `open-item`, use the platform's package upload control via
`browser-harness`, and verify the displayed package/version before leaving the
draft state. Uploading changes external state; do not submit for review here.

