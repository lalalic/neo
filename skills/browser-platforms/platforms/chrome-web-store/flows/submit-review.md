# Submit review

Status: `working` — browser-harness runner implemented; live account verification pending.

Preconditions: the requested package and listing changes are complete, and the
caller explicitly authorizes submission.

Review the platform's final summary, confirm the selected item and version,
submit once with `browser-harness`, and verify the resulting review/pending
state. Never submit merely because a draft is valid.
