---
name: wechat-channels-platform
description: "WeChat Channels browser-harness posting and manager assets."
---

# WeChat Channels

Use the parent `browser-platforms` contract and `browser-harness` first. The
`browser-harness/` directory is the canonical home for the migrated posting,
manager, and shadow-DOM helper mechanics used by `skills/post`.
