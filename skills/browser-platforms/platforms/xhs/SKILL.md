---
name: xhs-platform
description: "Xiaohongshu browser-harness posting and creator-manager assets."
---

# Xiaohongshu

Use the parent `browser-platforms` contract and `browser-harness` first. The
`browser-harness/` directory contains the canonical migrated mechanics used by
`skills/post`; posting-domain validation and publish semantics remain in `post`.
