# Publishing scripts

Publishing helpers belong here. A future implementation should validate the
canonical tree, update the Drive registry, publish the selected skill, and
verify the resulting file metadata and index.
