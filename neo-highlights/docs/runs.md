# Highlight events

Each directory here represents one candidate or approved Neo highlight video.
Use `../scripts/new-highlight.sh <slug>` rather than hand-copying templates.

The stable directory name is `YYYYMMDD-slug`. Keep its source facts in
`source.md`, Markcut authoring in `video.md`, local media in `assets/`, and the
review record in `REVIEW.md`.
